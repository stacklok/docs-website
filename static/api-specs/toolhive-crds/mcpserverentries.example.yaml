apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServerEntry
metadata:
  name: my-mcpserverentry
  namespace: default
spec:
  groupRef:
    name: <string>
  remoteUrl: https://example.com
  transport: sse
